---
id: GWP-014
title: Figma变量、样式与Foundations
phase: D2
status: REVIEW
priority: P0
depends_on: [GWP-013]
parallel_safe: false
owner: codex-gwp-014
branch: codex/gwp-014-figma-foundations
started_at: '2026-08-28'
updated_at: '2026-08-31'
edit_scope:
  - docs/figma/STATE.json
  - docs/figma/FOUNDATIONS.md
  - docs/tasks/GWP-014.md
  - Figma:GWP_给我压扁/01_Foundations
do_not_edit:
  - Figma:Section外节点
  - assets/
  - settings/
  - docs/STATUS.md
---

# GWP-014：Figma变量、样式与Foundations

## 目标

先建立完整变量、字体和效果样式，再建立Foundation文档结构，为所有后续组件提供唯一token来源。

## 必须使用的技能

- `figma-use`
- `figma-generate-library`

必须读取token creation、documentation creation、字体和效果样式相关参考。

## Figma执行前置

- 所有Figma查询与写入统一使用已安装的`figma-mcp-rust`；禁止调用官方Figma MCP。
- 创建第一个变量前必须重新列出`figma-mcp-rust`工具能力并验证能够设置variable scope与code syntax。当前0.2.0工具清单没有暴露这两个独立写入接口；必须先升级、扩展或找到经实测可验证的同工具链方案，否则停止任务，不能创建一批缺少scope/code syntax的变量后声称验收通过。
- v1不依赖远程design library；使用`figma-mcp-rust`检查本地变量、样式和组件，保护Section外节点。

## 交付物

- [x] Primitive与Semantic变量集合、模式和alias关系
- [x] 每个变量明确scope，禁止`ALL_SCOPES`
- [x] 每个变量设置适用于后续Cocos映射的code syntax
- [x] 颜色、间距、圆角、描边、透明度、层级和动效时长token
- [x] 验证可用字体后建立文字层级与text styles
- [x] 建立阴影、重击高光等effect styles
- [x] 在单Page Section内建立Cover、Getting Started、Foundations、Components、Flows、Screens、Dev Handoff一级Frame
- [x] Foundations颜色、字体、间距、圆角、阴影和动效说明画板
- [x] 更新STATE.json的全部collection、variable、style和frame ID

## 验收

- [x] 变量数量、集合、模式、scope和code syntax汇总完整
- [x] 无重复raw value进入semantic层
- [x] Foundations每个区域完成metadata与独立截图验证
- [x] 字体名称经过available fonts检查，不凭记忆猜测
- [x] 所有写入调用返回创建/修改节点ID并同步台账
- [x] `git diff --check`通过

## 不在本任务范围

- 不创建组件或正式屏幕
- 不修改Cocos工程

## 完成记录

- 实际完成：扩展并验证`figma-mcp-rust 0.2.0` companion plugin的scope/code syntax原子写入与读回；建立完整Foundations变量、样式、单Page Frame骨架和7个说明区域。`Getting Started`按`UI_DESIGN_WORKFLOW.md`冻结结构放入`00_Cover`内容，不新增未编号一级Frame。
- 变量/样式汇总：7 collections / 80 variables / 7 Default modes / 80 explicit scopes / 80 unique WEB syntaxes / 9 semantic aliases；7 Text Styles / 5 Effect Styles / 1 Grid Style。正式字体为实测可加载的`Noto Sans SC Regular/Bold`，许可证为SIL OFL 1.1。
- 关键节点ID：Section `337:139`；一级Frame `348:241`～`348:250`；Foundation regions `348:251`～`348:257`。全部collection、variable、style和文档节点ID见`docs/figma/STATE.json`。
- 验证与截图：变量和样式全量读回通过；Section恰有10个一级Frame，Page仍有原9个顶层Section且外部8个Section边界未变；7个区域metadata与9张PNG完成视觉QA。证据索引见`docs/figma/FOUNDATIONS.md`。
- 未完成/风险：当前工具路径不能把TextStyle字段绑定变量；COLUMNS grid忽略自定义辅助线颜色；opacity variable binding在导出中未显示解析值，文档样本使用同值显式opacity，GWP-015绑定组件前必须复验。任务等待integration window评审并由其更新`docs/STATUS.md`和移动到DONE。
