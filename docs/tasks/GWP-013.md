---
id: GWP-013
title: Figma工作区发现、范围锁定与状态台账
phase: D2
status: DONE
priority: P0
depends_on: [GWP-012]
parallel_safe: false
owner: codex-gwp-013
branch: codex/gwp-013-figma-discovery
started_at: '2026-08-28'
updated_at: '2026-08-28'
edit_scope:
  - docs/figma/
  - docs/design/FIGMA_GAP_ANALYSIS.md
  - docs/tasks/GWP-013.md
  - Figma:GWP_给我压扁
do_not_edit:
  - Figma:Section外节点
  - assets/
  - settings/
  - docs/STATUS.md
---

# GWP-013：Figma工作区发现、范围锁定与状态台账

## 目标

在任何Figma设计创建前，检查共享文件、库、现有组件和编辑边界，锁定v1 token/组件范围，并建立可跨窗口恢复的节点状态台账。

## 必须使用的技能

- `figma`
- `figma-use`
- `figma-generate-library`

调用任何Figma工具前必须完整读取这些技能及其本阶段要求的Discovery参考。目标文件已存在，不调用`create_new_file`。

## 交付物

- [x] 只读检查目标文件、共享Page、锚点`11:7101`和Section外节点
- [x] 调用`get_libraries`，分页检查可用库后再执行按意图拆分的`search_design_system`
- [x] 检查本地变量、远程变量、组件、样式、字体和命名约定
- [x] 输出`FIGMA_GAP_ANALYSIS.md`：可复用、需包装、需新建以及所有冲突
- [x] 锁定v1变量、样式和组件精确清单，并取得用户确认
- [x] 在共享Page内创建唯一顶层Section `GWP_给我压扁`，不触碰相邻节点
- [x] 建立`docs/figma/STATE.json`，记录file key、Page锚点、Section ID、run ID、阶段、节点ID、验证和待办
- [x] 截图确认Section位置和编辑边界

## 验收

- [x] 用户确认gap analysis和v1设计系统范围
- [x] 所有Figma写入严格串行、增量执行并返回节点ID
- [x] 新Section不覆盖、移动或重命名任何已有项目节点
- [x] STATE.json可让新窗口通过只读检查恢复上下文
- [x] metadata与截图验证通过
- [x] `git diff --check`通过

## 不在本任务范围

- 不创建变量、组件或正式屏幕
- 不修改Cocos工程

## 完成记录

- 实际完成：完成共享文件、Page、顶层边界、本地变量/组件/样式/字体与已添加 libraries 的只读发现；输出 gap analysis 并锁定 80 个变量、7 个 Text Style、5 个 Effect Style、1 个 Grid Style、70 个组件的 v1 范围；建立可恢复状态台账；在确认后只创建一个项目 Section。
- Figma文件/Section：`mini-game` / `shanliyoubao`；`GWP_给我压扁`（`x=20050, y=0, 6400×14000`）。
- 关键节点ID：Page `11:7101`；Section `337:139`。创建前 8 个其他项目顶层节点均只读保留。
- 验证与截图：最终 Page depth-1 metadata 显示 9 个顶层 Section，新 Section 唯一、为空且边界精确；与原最右边界 `x=19650` 保持 400px 间距；截图 `docs/figma/screenshots/gwp-013-section-337-139.png` 成功导出。PageNode 不支持整页 export，因此边界由顶层 metadata 证明，截图用于验证 Section 自身。
- 用户确认：2026-08-28，用户回复“插件已连接，确认范围”。
- 集成验收：2026-08-28，集成窗口使用 `figma-mcp-rust 0.2.0` 重新连接本地插件并独立复核。文件 `mini-game` 当前 Page 为 `shanliyoubao`（`11:7101`）；depth-1 metadata 确认 9 个顶层 Section、`GWP_给我压扁` 仅有一个、节点 `337:139` 为空且边界为 `20050,0,6400×14000`，与原内容保留 400px；当前重新导出的 Section 截图与仓库截图 SHA-256 完全一致。变量、样式、组件和字体计数与台账一致，提交范围和 `git diff --check` 通过；提交 `f0bacfb` 已合入 `main`。
- 未完成/风险：官方 Starter MCP 限额阻断了远程组件搜索的完整返回；未把错误解释为空结果，且确认后的 v1 不依赖远程库。正式字体家族、字重可加载性与授权由 GWP-014 决定。
