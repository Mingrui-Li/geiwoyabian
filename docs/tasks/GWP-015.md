---
id: GWP-015
title: Figma基础交互组件
phase: D3
status: DONE
priority: P0
depends_on: [GWP-014]
parallel_safe: false
owner: codex-gwp-015
branch: codex/gwp-015-base-components
started_at: '2026-08-31'
updated_at: '2026-09-01'
edit_scope:
  - docs/figma/STATE.json
  - docs/figma/COMPONENTS.md
  - docs/figma/tooling/figma-mcp-rust-gwp014/
  - docs/tasks/GWP-015.md
  - docs/STATUS.md
  - Figma:GWP_给我压扁/02_Components/Base
do_not_edit:
  - Figma:Section外节点
  - assets/
  - settings/
---

# GWP-015：Figma基础交互组件

## 目标

逐个建立后续所有屏幕依赖的基础交互组件，保证变量绑定、Auto Layout、变体和属性完整。

## 必须使用的技能

- `figma-use`
- `figma-generate-library`

必须读取component creation与component patterns参考。每次只完成一个组件，验证通过后才能开始下一个。

## 组件范围

- [x] Button：Primary/Secondary/Danger，适用尺寸与Default/Pressed/Disabled/Loading
- [x] IconButton：普通、强调、关闭/返回用途，含Pressed/Disabled
- [x] Tab/Segment：Default/Selected/Disabled
- [x] Badge/StatusPill：普通、成功、警告、锁定、完成
- [x] Progress：线性进度、星级进度和加载状态
- [x] Toast/InlineMessage：普通、成功、错误和断网
- [x] Loading：启动、局部和按钮加载

## 每个组件的完成要求

- [x] 先查询可复用组件；复用、包装或新建的理由记录在COMPONENTS.md
- [x] 使用Auto Layout和设计变量，无硬编码颜色、间距、圆角和阴影
- [x] variant矩阵受控，图标使用INSTANCE_SWAP而不是每图标一个variant
- [x] 文本、布尔和实例交换属性连接到正确子节点
- [x] 描述、用法和禁止场景写入组件说明
- [x] metadata与独立screenshot通过
- [x] 节点ID和验证状态同步到STATE.json

## 不在本任务范围

- 不组装正式屏幕
- 不创建游戏专用HUD组件
- 不修改Cocos工程

## 完成记录

- 实际完成：在 `02_Components/Base` 逐项建立 7 个基础组件集、8 个内部图标原子与对应说明框架；补齐仓库伴生插件的 Variant、组件属性、变量绑定、语义预设与结构审计能力。Button、IconButton、StatusPill 在截图复核后完成语义默认值和 Disabled/Locked 对比度修复。
- 组件/变体汇总：Button 24、IconButton 24、SegmentedControl 21、StatusPill 10、Progress 12、Toast/InlineMessage 8、Loading 3；合计 102 个受控 Variant。
- 关键节点ID：Components `352:389`；Base `352:390`；Component Set `352:516`、`352:623`、`352:866`、`352:911`、`352:978`、`353:1023`、`353:1052`；完整说明见 `docs/figma/COMPONENTS.md` 与 `docs/figma/STATE.json`。
- 验证与截图：能力探针通过后删除；每个创建结果均为 0 硬编码 Paint、0 组件根 Auto Layout 失败；最终扫描为 7 个 GWP Component Set + 8 个内部 Component；7 张独立截图通过目视检查；Section 外 8 个既有节点 ID 与边界保持不变。伴生插件 TypeScript 检查与 Vite 生产构建通过，构建产物哈希匹配；`STATE.json` 解析、截图存在性与 `git diff --check` 通过。
- 未完成/风险：无 GWP-015 阻塞项。Figma opacity 绑定已在组件上重新验证；Text Style 字段绑定和 column-grid 自定义颜色仍沿用 GWP-014 已记录的工具限制，不影响本任务组件冻结。
